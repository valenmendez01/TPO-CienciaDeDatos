import { Note } from "@cdd/charts";

/** Nota al margen con caveat metodológico. */
export const Caveat = () => (
  <div style={{ maxWidth: 540 }}>
    <Note>
      La severidad individual por edad la dominan los mayores de 65 (fragilidad), no
      los jóvenes. Exposición y vulnerabilidad son mecanismos distintos.
    </Note>
  </div>
);
