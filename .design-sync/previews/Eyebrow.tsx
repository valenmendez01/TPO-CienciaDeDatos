import { Eyebrow } from "@cdd/charts";

/** Cintillo con número de sección y línea. */
export const Clasificacion = () => <Eyebrow num="05" section="Clasificación · clases 7–8" />;

/** Sin número (solo etiqueta de sección). */
export const SinNumero = () => <Eyebrow section="Aprendizaje no supervisado" />;
